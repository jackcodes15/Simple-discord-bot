def get_response(message: str) -> str:
    p_message = message.lower()

# Capitalisation will not matter with messages.

# Example message below on the format you should use.

    if p_message == 'Hello':
        return 'Hello to you too!'